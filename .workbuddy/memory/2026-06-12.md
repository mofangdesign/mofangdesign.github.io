# 2026-06-12 工作日志

## 图片迁移到 imgurl.org 国内图床

- imgurl.org API 信息：
  - 端点：`https://www.imgurl.org/api/v2/upload`
  - UID: `uakppr`，Token: `sk-gpCcdtUCcgOUaJHEDT0v3ZeNSDmA0lCWtOgSy1WH79IHeuWI1BEUw3qzLWKFc`
  - 图片存储域名：`https://s3.bmp.ovh/`
  - **免费版限制：每日 29 张**（实测，文档写 10 张但实际多些）

- 已上传 29/48 张图片，映射存储在 `mofang-showcase/imgurl_map.json`
- 剩余 19 张未上传（被 daily.limit.exceeded 拦截）：
  - 自建房别墅 第16页-33.jpg
  - 自建房外观（3张）
  - 自建房外观设计（5张）
  - 西方大院（3张）
  - 金海岸（2张）
  - 雅居乐上善若水（2张）
  - 花都别墅（3张）

- 已将 IMGURL_MAP 嵌入 index.html，添加 resolveUrl() 函数
  - 已上传的图片 → s3.bmp.ovh 国内加速
  - 未上传的图片 → jsDelivr CDN 兜底
- index.html 已推送到 GitHub Pages（commit: 926ff5e4）

## 手机端修复（commit: da362249）
- lightbox 导航：桌面保留左右浮动按钮；手机改为底部横排按钮 + 手指左右滑动切图
- 末尾缺角：CSS `.portfolio-item:last-child:nth-child(odd)` 拉伸到 `grid-column: 1 / -1`，宽屏比 16:7
- 图片懒加载：rootMargin 从 100px 提升到 400px，提前约一屏加载

## 手机端点击无反应修复（commit: a2d4a938）
- 根本原因：手机端把 `.lightbox-nav` 改为 position:static 后，桌面浮动按钮乱跑遮挡内容
- 修复方案：手机端用 `#lightboxPrev, #lightboxNext { display:none !important }` 隐藏桌面按钮，只保留底部 mobile 按钮
- 加 `touch-action: manipulation` 消除 portfolio-item 和 lightbox-close 的 300ms 点击延迟
- 手机端 `.lightbox-content` 加 `overflow-y:auto` 确保内容可滚动

## 工艺展示页 AI 图片部署（23:00 — imgurl.org 日限额已满，改用 GitHub + jsDelivr）
- 5 张 AI 生成的工艺图片已上传到 GitHub 仓库（`mofang-showcase/cases/工艺展示/`）：
  - craft-01-dianshui.png（1163KB，水电施工）
  - craft-02-niwa.png（1174KB，泥瓦铺贴）
  - craft-03-mugong.png（1058KB，木工造型）
  - craft-04-tuzhuang.png（1035KB，涂装打磨）
  - craft-05-baohu.png（1035KB，成品保护）
- index.html 中 5 个 SVG placeholder 已替换为 `<img src="jsDelivr URL" loading="lazy">`
- index.html 已推送到 GitHub Pages
- jsDelivr CDN 已确认可访问所有 5 张图片
- 上传脚本：`mofang-showcase/upload_craft_to_github.py`

## 工艺展示图片替换为实拍图（22:40）
- 用户不满意 AI 生成的工艺图片，用自己桌面 `工艺图片/` 文件夹中的实拍图替换：
  - 电工.jpg（924KB）→ craft-01-dianshui.jpg（水电）
  - 瓦工.jpg（378KB）→ craft-02-niwa.jpg（泥瓦）
  - 木工.jpg（232KB）→ craft-03-mugong.jpg（木工）
  - 油漆.jpg（92KB）→ craft-04-tuzhuang.jpg（涂装）
  - 地面保护.jpg（131KB）→ craft-05-baohu.jpg（成品保护）
- 用户先后更新了油漆和地面保护两张图
- index.html 中 5 个 img src 从 jsDelivr URL 改为相对路径 `cases/工艺展示/craft-XX.jpg`
- 所有图片通过 GitHub API 上传并确认 GitHub Pages 可访问

## 自建房别墅图片加载修复（22:42）
- 问题：portfolioData 中图片路径为 `.PNG`（大写），实际文件是 `.jpg`
- 修复：将所有 6 张图片路径从 `.PNG` 改为 `.jpg`
- index.html 已推送（commit: 4e932a5bb44a）

## 待办
- **明天继续上传剩余 19 张案例图片**到 imgurl.org，运行 `upload_to_imgurl.py`（会自动跳过已上传的）
- 上传完成后更新 index.html 中 IMGURL_MAP 数据

